import { HeaderContainer, HeaderContent, Logo } from "./style";

export const Header = () => {
    return (
        <HeaderContainer>
            <HeaderContent>
                <Logo>ODIN</Logo>
            </HeaderContent>
        </HeaderContainer>
    );
};